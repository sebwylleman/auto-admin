#/bin/bash

# This script requires a file to read as an argument.
# It counts the number of failed login attempts by IP address and produces output in CSV format.
# Displays: any IP addresses with more than 10 failed login attempts, the number of attempts made, the IP address from which
# those attempts were made, and the location of the IP address.

